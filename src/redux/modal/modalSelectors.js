export const selectIsOpenModal = (state) => state.modal.isOpenModal;
export const selectModalData = (state) => state.modal.modalData;
export const selectIsApproveModalOpen = (state) =>
  state.modal.isApproveModalOpen;
export const selectIsModalEditUserOpen = (state) =>
  state.modal.isModalEditUserOpen;
